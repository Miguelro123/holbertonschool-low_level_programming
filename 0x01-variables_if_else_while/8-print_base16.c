#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c;
	char d;

	for (c = '0' ; c <= '9' ; c++)
	{

		putchar(c);

	}

	for (d = 'a' ; d <= 'z' ; d++)
	{

		putchar(c);
	}
	putchar('\n');
	return (0);
}
